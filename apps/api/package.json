{
  "name": "@nanodb/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "start": "bun dist/index.js"
  },
  "dependencies": {
    "@nanodb/db": "workspace:*",
    "@nanodb/auth": "workspace:*",
    "@nanodb/ai": "workspace:*",
    "@prisma/client": "^7.5.0",
    "better-auth": "^1.5.5",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "prisma": "^7.5.0"
  }
}
